#include "main.h"
/**
 * mul - Multiplie deux entiers.
 * @a: Le premier entier.
 * @b: Le deuxième entier.
 *
 * Return: Le résultat de la multiplication (a * b).
 */
int mul(int a, int b)
{
return (a * b);
}
