#include <stdio.h>
#include <stdlib.h>
/**
 * main - affiche l'alphabet sauf la lettre e et q
 *
 * Return:0 Si rien ne s'affiche tout est parfait
 */
int main(void)
{
	char nb;

	for (nb = '0'; nb <= '9'; nb++)
	{
		putchar(nb);
	}
		putchar('\n');
		return (0);
}
